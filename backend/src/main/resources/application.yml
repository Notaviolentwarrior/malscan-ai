server:
  port: 8080

spring:
  application:
    name: malscan-backend

  data:
    mongodb:
      uri: mongodb://localhost:27017/malscan_ai

  servlet:
    multipart:
      max-file-size: 25MB
      max-request-size: 25MB

app:
  upload-dir: uploads/quarantine
